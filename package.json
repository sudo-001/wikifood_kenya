{
  "name": "wikifood-kenya",
  "version": "1.0.0",
  "description": "\"a wikidata clone but for food, here just for the kenya\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "style": "npx tailwindcss -i ./src/style/input.css -o ./src/style/style.css --watch"
  },
  "author": "sudo",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  },
  "dependencies": {
    "sparqljs": "^3.7.1"
  }
}
